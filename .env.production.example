# =============================================================================
# AI Olympics - Production Environment Configuration
# =============================================================================
# Copy this file to .env.production and fill in real values.
# NEVER commit the actual .env.production file to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=3003

# Frontend URL (used for CORS allowlist and Stripe redirect URLs)
CLIENT_URL=https://your-production-domain.com

# Backend API URL (used by frontend proxy and task URLs)
API_URL=https://api.your-production-domain.com

# -----------------------------------------------------------------------------
# Supabase (Required)
# -----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# Encryption key for API keys stored in DB (32 bytes hex)
# If not set, derived from SUPABASE_SERVICE_KEY. For production, generate one:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# API_KEY_ENCRYPTION_KEY=your-64-char-hex-string

# -----------------------------------------------------------------------------
# AI Model API Keys
# -----------------------------------------------------------------------------

# Option 1: OpenRouter (recommended - single key for all models)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key

# Option 2: Individual provider keys (if not using OpenRouter)
# Anthropic: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your-anthropic-api-key

# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# Google AI: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your-google-ai-api-key

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------

# JWT secret for signing tokens (min 32 characters, random string)
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-jwt-secret-min-32-chars

# GitHub OAuth (for agent repository integration)
# Create at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-oauth-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret

# -----------------------------------------------------------------------------
# Stripe (Payments & Wallet)
# -----------------------------------------------------------------------------
# Get from: https://dashboard.stripe.com/apikeys
# IMPORTANT: Use live keys for production, test keys for staging
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-publishable-key

# Webhook signing secret (from Stripe webhook endpoint config)
# Set up at: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret

# -----------------------------------------------------------------------------
# Blockchain / Prediction Markets
# -----------------------------------------------------------------------------

# Polygon RPC URL (for on-chain prediction market settlement)
# Get from: https://www.alchemy.com/ or https://www.infura.io/
POLYGON_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/your-key

# Platform wallet (for market settlement transactions)
# CRITICAL: Keep private key extremely secure. Consider using a KMS instead.
# PLATFORM_WALLET_ADDRESS=0xyour-wallet-address
# PLATFORM_WALLET_PRIVATE_KEY=your-private-key

# Kalshi credentials (for prediction market integration)
# KALSHI_EMAIL=your-kalshi-email
# KALSHI_PASSWORD=your-kalshi-password

# Feature flags for market trading
# POLYMARKET_CLOB_ENABLED=false
# KALSHI_TRADING_ENABLED=false

# -----------------------------------------------------------------------------
# Voice & Media
# -----------------------------------------------------------------------------

# ElevenLabs (AI commentary voice synthesis)
# Get from: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# -----------------------------------------------------------------------------
# Streaming (Optional)
# -----------------------------------------------------------------------------

# OBS WebSocket (for live streaming integration)
# Only needed if streaming competitions live
# OBS_WEBSOCKET_URL=wss://your-obs-host:4455
# OBS_WEBSOCKET_PASSWORD=your-obs-password

# -----------------------------------------------------------------------------
# Docker (Production sandboxing)
# -----------------------------------------------------------------------------
# DOCKER_SOCKET=/var/run/docker.sock

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=warn

# =============================================================================
# Frontend Environment Variables (set in frontend/.env or Vercel dashboard)
# =============================================================================
# These use the VITE_ prefix and are embedded at build time.
# Set them in your deployment platform (e.g., Vercel Environment Variables).
#
# VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
# VITE_API_URL=https://api.your-production-domain.com
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-publishable-key
