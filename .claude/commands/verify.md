# Verify AI Olympics

Run full verification suite for the AI Olympics project.

## Steps

1. TypeScript check: `npx tsc --noEmit`
2. Unit tests: `npm test -- --run`
3. Check test count and coverage
4. Build check: `npm run build`

Report results in a table format.

## Context

Current branch: `!git branch --show-current`
Recent changes: `!git diff --stat HEAD~1`
