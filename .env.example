# AI Olympics Environment Configuration

# Server
PORT=3003
NODE_ENV=development

# =============================================================================
# SUPABASE (Required for Marketplace features)
# =============================================================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Encryption key for API keys stored in DB (32 bytes hex, auto-derived from service key if not set)
# API_KEY_ENCRYPTION_KEY=your-32-byte-hex-key

# Frontend URL for CORS allowlist
CLIENT_URL=http://localhost:5173

# =============================================================================
# AI Model API Keys
# =============================================================================

# Option 1: Use OpenRouter (recommended - provides access to ALL models)
OPENROUTER_API_KEY=your-openrouter-key

# Option 2: Use individual API keys (if not using OpenRouter)
ANTHROPIC_API_KEY=your-anthropic-key
OPENAI_API_KEY=your-openai-key
GOOGLE_AI_API_KEY=your-google-ai-key

# =============================================================================
# Streaming & Integration
# =============================================================================

# OBS WebSocket (for live streaming)
OBS_WEBSOCKET_URL=ws://localhost:4455
OBS_WEBSOCKET_PASSWORD=your-obs-password

# Docker (for sandboxed execution)
DOCKER_SOCKET=/var/run/docker.sock

# =============================================================================
# Optional: External Services
# =============================================================================

# Prediction Markets
# Polymarket - No auth needed (public API)
# Kalshi - Requires account credentials
KALSHI_EMAIL=your-kalshi-email
KALSHI_PASSWORD=your-kalshi-password

# ElevenLabs (for AI commentary voice)
ELEVENLABS_API_KEY=your-elevenlabs-key

# Logging
LOG_LEVEL=debug

# =============================================================================
# PRODUCTION REQUIRED (set these when deploying to production)
# =============================================================================

# JWT secret for auth tokens (64+ char random string, REQUIRED in production)
# JWT_SECRET=your-64-char-random-string-here

# Encryption key for stored API keys (32 bytes hex, REQUIRED in production)
# API_KEY_ENCRYPTION_KEY=your-32-byte-hex-key

# Sentry error tracking (recommended for production)
# SENTRY_DSN=https://your-dsn@sentry.io/project-id

# Sentry trace sample rate (0.0-1.0, default 0.2 in production)
# SENTRY_SAMPLE_RATE=0.1

# Feature flag: real-money trading (disabled by default)
# ENABLE_REAL_MONEY_TRADING=false

# Stripe payments (required if real-money trading enabled)
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Redis for competition state persistence (recommended for production)
# REDIS_URL=redis://localhost:6379

# Market data sync (background ingestion from Polymarket + Kalshi)
# ENABLE_MARKET_SYNC=true
